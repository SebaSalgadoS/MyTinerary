import { text } from "react-native"

export const TextCustom = () => {

    const color2 = props.color2
    return (
        <Text 
        numberOfLines={1} 
        style={{fontSize: 20, }}
        >
        (prompt.children)
        </Text>
    );
};